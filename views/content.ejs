<!DOCTYPE html>
<html>
  <head>
    <title>Receive</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  <%- include('partials/tailwind') %>
  </head>
  <body class="space-y-4 container mx-auto">
    <h1 class="text-3xl font-bold">Decrypted Text</h1>


<% if (decryptedContent && decryptedContent.length > 0) { %>
    <section class="space-y-2">
 <% decryptedContent.forEach(function(paragraph) { %>
        <p class="mb-8"><%= paragraph %></p>
    <% }); %>
    </section>

<% } %>
  </body>
</html>
